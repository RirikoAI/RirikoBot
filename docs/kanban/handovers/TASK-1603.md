# Handover Note: [TASK-1603] Crafting Docs, Catalog Copy & Help Center Entry

- **Ticket Type & Points**: Task | 1 pt
- **Parent**: `STORY-160` (Equipment Crafting with Dust)
- **Author / Agent**: Claude Code (Sonnet 5)
- **Status**: DONE
- **Timestamp**: 2026-09-21T00:00:00Z

## 1. Summary of Work Accomplished

- [docs/waifu-tcg.md](file:///Z:/Projects/ririko-v2-2026/docs/waifu-tcg.md): added **§10.4 "Crafting: Forging Gear
  and Potions from Dust"**, right after §10.3 (Equipment Enhancement) and before §11 (Consumables). Covers: what can
  be crafted (six per-slot upgrade chains plus 2 potion recipes), why boss signature drops are permanently excluded,
  the exact cost formula for credits/dust, the ingredient/melt-down rule, unlock floors per rarity bracket, quantity
  caps, and the command surface (`/item action:craft`, prefix `craft`/`forge`, the three ways to type a recipe code).
  Ends with a 26-row recipe table.
  - Stale-line fixes: §10.3 (`~line 412`) now says Crafting Dust is *also* spent to forge gear/potions outright, and
    also fixed "earned by dismantling duplicate waifu cards" to include "or by clearing dungeon floors" (dust drops
    from floor clears too, per `dungeon-loot.service.ts` — was already true, just undocumented). §6.2
    `/card dismantle` bullet (`~line 166`) now mentions crafting as a third use of dust. §11.1's "Major HP Potion:
    ...Crafted or dropped in Dungeons" (`~line 433`) needed no change — it was already accurate.
  - **Recipe table numbers are generated, not hand-typed.** Wrote a throwaway script
    ([dump-recipes.ts](file:///C:/Users/fariz/AppData/Local/Temp/claude/Z--Projects-ririko-v2-2026/e8c74a92-358d-4c15-b02d-743049e3b82b/scratchpad/dump-recipes.ts),
    scratchpad only, not committed) that dynamically imports the real
    `crafting-recipes.ts`/`catalog.ts` via `tsx` and prints every recipe's code/name/rarity/slot/floor/dust/
    credits/ingredient straight from `CRAFTING_RECIPES`. The doc table is a direct copy of that output — if the
    recipe table ever drifts from the code, re-run that pattern (the script itself wasn't kept; it's a 25-line
    `pathToFileURL` + dynamic-import snippet, trivial to redo).
- [packages/services/src/waifu-tcg/equipment/catalog.ts](file:///Z:/Projects/ririko-v2-2026/packages/services/src/waifu-tcg/equipment/catalog.ts):
  `CRAFTING_DUST`'s description changed from "...Spent to enhance equipment." to "...Spent to enhance equipment or
  craft new gear and potions." — copy only, no other field touched.
  - **Checked whether this reaches existing DBs — it does not.** `apps/bot/src/services.ts` (~line 674-687) seeds
    `CANONICAL_ITEMS` with `if (!exists) await gameItemRepo.create(item)` — insert-only, keyed by `findByCode`. Any
    DB that has already booted once (dev or prod) already has a `CRAFTING_DUST` row with the old description and
    will keep it forever; only a brand-new/never-seeded DB picks up the new text. No migration or `UPDATE` was
    added — the task scope was "check and note," not "make it reach existing DBs," and changing the seed loop to
    upsert is a behavior change outside this task's description-only mandate. Flagging as a possible follow-up if
    the team wants the flavor text to actually update in production.
  - **Checked for tests asserting the exact string** (`grep -rn "Spent to enhance equipment\|Shimmering residue"`)
    — zero hits outside `catalog.ts` itself. No test needed updating.
- [docs/commands.md](file:///Z:/Projects/ririko-v2-2026/docs/commands.md) §5.3: added a `/item craft [recipe]
  [quantity]` bullet directly under the existing `/item use` bullet (the section already lists `inventory` and
  `use`; `enhance` was never listed there either — pre-existing gap, not introduced or touched by this task).
- **In-code help center**: no change needed, confirmed again (TASK-1602's handover already established this).
  `packages/discord/src/help/command.ts` has zero references to `enhance`/`craft`/action names — it generates its
  views live from `CommandRegistry`/`command.metadata`, and `item.command.ts`'s `craft` choice + `recipe`/`quantity`
  options + updated `examples` (added in TASK-1602) are already picked up automatically.
- **Grepped every place `enhance` appears under `docs/`** to find every command-inventory doc that might need a
  parallel `craft` entry (per the task instructions): `docs/waifu-tcg.md` (updated, see above),
  `docs/commands.md` (updated, see above), `docs/database.md` (schema/ACID prose only, no command list),
  `docs/tcg-player-guide.md` (has an Enhancement System section at §4 and a "Command Quick Reference Cheat Sheet" at
  §10, but that cheat sheet lists zero `/item` actions at all — not `inventory`, `use`, or `enhance` — so it's a
  pre-existing gap outside this task's "if other item actions are listed there" condition; left untouched),
  `docs/legacy-feature-inventory.md` (its one "enhance" hit is the generic word in the REWORK action-legend
  definition, unrelated to the `/item` command), `docs/waifu-tcg-implementation_plan.md` and
  `docs/implementation-roadmap.md` (both historical planning docs describing what TASK-1031 already shipped, not
  live command references — left untouched), and the various `docs/kanban/handovers/*.md` files (historical, never
  touched retroactively).

## 2. Current State & Verification

- `pnpm typecheck` (all 8 workspace packages + both apps) — **clean**.
- `pnpm -F @ririko/services exec vitest run src/waifu-tcg` — **243/243 passed**, no regressions (only a description
  string changed; no logic touched).
- `pnpm -F @ririko/bot exec vitest run src/commands/tcg` — **82/82 passed**, no regressions.
- `pnpm -F @ririko/services exec eslint src/waifu-tcg/equipment/catalog.ts` — **0 errors/warnings**.
- Full suite: `pnpm test` — **139/139 test files passed, 1308/1308 tests passed**, no flaky failures on this run
  (the known live-network music extractor tests and occasional DB timeouts under parallel load did not manifest
  this time; no rerun was needed).
- Git branch: `feat/STORY-160-equipment-crafting`. Nothing committed or pushed, per instructions — the user decides
  on commit/PR.
- No behavior change beyond the two description strings (`catalog.ts`'s dust flavor text, doc prose) — verified by
  the full test suite passing unchanged and by `git diff` only touching `.md` files plus the one-line
  `catalog.ts` description.

## 3. Roadblocks, Gotchas & Decisions Made

- **No real bug found** — this task stayed pure documentation/copy as scoped. The one "decision" worth flagging is
  the catalog-seed insert-only behavior (see above): it's existing, correct-by-design behavior (canonical items are
  meant to be created once, not clobbered if an admin hand-edits a row later), not a defect, so it wasn't changed.
- The recipe table is hand-pasted from a one-off script's output rather than generated at doc-build time (this repo
  has no doc-generation pipeline). If `crafting-recipes.ts` or the underlying `catalog.ts` prices/rarities change,
  the table in `docs/waifu-tcg.md` §10.4 will silently drift — worth a `pnpm docs:verify-recipes` script someday,
  but out of scope for a 1-point task.
- Confirmed the WIP invariant before starting: `active_in_progress_ticket` was `null` in `board.json` at session
  start (TASK-1602's handover left it that way), so TASK-1603 moved straight to `IN_PROGRESS` with no
  interruption/pause conversation needed.

## 4. Actionable Next Steps

- STORY-160 (Equipment Crafting with Dust) is now feature-complete and documented, and moves to `REVIEW`. See
  [STORY-160.md](file:///Z:/Projects/ririko-v2-2026/docs/kanban/handovers/STORY-160.md) for the story-level summary.
- Per protocol §2.5 (Anti-Runaway Session Boundary): **stop and ask the user** whether they want to commit, push
  `feat/STORY-160-equipment-crafting`, and open a PR to `develop/2.0.0` before starting any further work.
- Suggested pre-merge step: a live Discord playtest of `/item action:craft` (menu and direct-code paths) and
  `!item craft`/`!item forge` prefix parity — nothing in this branch has been run against a live bot yet, only
  vitest.
