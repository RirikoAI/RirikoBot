# Handover Note: [TASK-0231] Legacy SQLite Inspector & 17-Entity Data Transformer

- **Ticket Type & Points**: Task | 2 pts
- **Author / Agent**: lead-architect / database / migration
- **Status**: DONE
- **Timestamp**: 2026-09-15T08:52:00+08:00
- **Parent Story**: [`STORY-023: Legacy 1.4.0 SQLite Migration Engine & CLI`](file:///z:/Projects/ririko-v2-2026/docs/kanban/BOARD.md)

---

## 1. Summary of Work Accomplished
- **Zero Legacy Mutation Invariant & Read-Only Inspector ([`packages/database/src/migration/inspector.ts`](file:///z:/Projects/ririko-v2-2026/packages/database/src/migration/inspector.ts))**:
  - Implemented `LegacySqliteInspector` opening the legacy 1.4.0 database exclusively with `{ readonly: true, fileMustExist: true }`.
  - Audits all 17 legacy tables: `user`, `guild`, `guild_config`, `configuration`, `user_note`, `voice_channel`, `music_channel`, `playlist`, `track`, `stream_subscription`, `stream_notification`, `twitch_streamer`, `reaction_role`, `reminder`, `free_game_notification`, `item`, `item_category`.
  - Calculates pre-migration balance sums (`totalCoins`, `totalKarma`) and counts total users and guilds.
  - Automatically identifies anomalies (e.g. negative balances, empty user IDs).
- **17-Entity Normalized Data Transformer ([`packages/database/src/migration/transformer.ts`](file:///z:/Projects/ririko-v2-2026/packages/database/src/migration/transformer.ts))**:
  - Maps 1.4.0 TypeORM entities to 2.0.0 Drizzle models with zero data loss:
    - `user` -> `users`, `economy_balances`, `xp_accounts`, and initial `economy_transactions` (`type: 'MIGRATION_V1'`, `source: 'LEGACY_MIGRATION'`).
    - `guild` & `guild_config` -> `guilds` and pivoted `guild_settings` (welcomer/farewell channels, backgrounds, toggles, prefix).
    - `user_note` -> `moderation_notes`.
    - `voice_channel` -> `auto_voice_configs`.
    - `music_channel` -> `music_channels`.
    - `playlist` & `track` -> `music_saved_playlists` and sequential `music_playlist_tracks`.
    - `stream_subscription` & `twitch_streamer` -> `stream_subscriptions` and `streamers` (`platform = 'TWITCH'`).
    - `reaction_role` -> `reaction_roles`.
    - `reminder` -> `reminders`.
    - `free_game_notification` -> `free_game_announcements`.
    - `item` & `item_category` -> `economy_items` and `economy_item_categories`.
    - `configuration` -> securely bypassed (plaintext secrets deprecated).
- **Test Harness ([`packages/database/src/migration/migration.test.ts`](file:///z:/Projects/ririko-v2-2026/packages/database/src/migration/migration.test.ts))**:
  - Validated inspector table counting and anomaly detection.
  - Validated transformer entity outputs and exact currency sum conservation (`1800n === 1800n`).

---

## 2. Current State & Verification
- Branch: `feat/STORY-023-legacy-migration`
- Quality Gates:
  - `pnpm run build`: Passed (0 errors across composite references)
  - `pnpm run typecheck`: Passed (0 errors across all 5 workspace projects)
  - `pnpm run lint`: Passed (0 warnings, 0 errors)
  - `pnpm run format:check`: Passed
  - `pnpm run test`: **53 / 53 tests passed** across 11 test suites

---

## 3. Actionable Next Steps
- Transition `TASK-0231` to `DONE` and `TASK-0232: ririko migrate:legacy CLI Command with Dry-Run & Verification Harness` (1 pt) to `IN_PROGRESS` (Strict WIP = 1).
- Implement:
  - `MigrationEngine` (`packages/database/src/migration/engine.ts`) with chunked transaction batches.
  - CLI subcommand `ririko migrate:legacy` and `ririko migrate:verify` in `apps/cli/src/commands/migrate.ts`.
  - Comprehensive CLI migration test.
