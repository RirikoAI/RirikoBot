# Handover Note: [TASK-1404] Dual-Dispatch Commands (/autorole, /create-reaction-role, /reaction-roles, /temprole) with Slash & Prefix Parity

## 1. Summary of Changes
- **Bot Services Initialization (`apps/bot/src/services.ts`)**:
  - Wired `reactionRoleRepo`, `autoRoleRepo`, `reactionRoleService`, and `autoRoleService` into `BotServices` interface and `createBotServices()`.
- **Database Repository Enhancement (`packages/database/src/repositories/autorole.repository.ts`)**:
  - Added `listTemporaryRoles(guildId)` to query all active temporary roles in a guild.
- **Commands Implementation**:
  - `apps/bot/src/commands/roles/create-reaction-role.command.ts`:
    - Full slash and prefix support for creating emoji reaction roles and Discord button components.
    - Supports modes (`TOGGLE`, `GIVE_ONLY`, `REMOVE_ONLY`, `UNIQUE`) and radio groups (`group`).
    - Enforces `Administrator` / `ManageRoles` permission and bot role hierarchy.
  - `apps/bot/src/commands/roles/reaction-roles.command.ts`:
    - Lists configured reaction roles in an embed (`list` subcommand or default prefix).
    - Removes reaction roles by ID (`remove <id>`), cleaning up bot reactions if possible.
  - `apps/bot/src/commands/roles/autorole.command.ts`:
    - Manages automatic join roles (`humans <role>`, `bots <role>`).
    - Configures verification gateway role (`verify <role>`).
    - Displays current settings (`show`).
    - Disables autoroles (`disable <target>`).
    - Posts interactive verification embed with button (`send-verify [channel] [message]`).
  - `apps/bot/src/commands/roles/temprole.command.ts`:
    - Assigns temporary expiring roles (`add <user> <role> <duration>`) with human-friendly duration parser (`parseRoleDuration` supporting `s`, `m`, `h`, `d`, `w`).
    - Removes temporary roles (`remove <user> <role>`).
    - Lists active temporary roles (`list [user]`) with Discord dynamic timestamps (`<t:unix:R>`).
  - `apps/bot/src/commands/roles/index.ts`:
    - Exports `createRoleCommands(services: BotServices): Command[]`.
- **Registration & Integration**:
  - Exported from `apps/bot/src/index.ts`.
  - Registered commands in `apps/bot/src/main.ts`.

## 2. Verification
- `apps/bot/src/commands/roles/__tests__/role-commands.test.ts`:
  - 15/15 tests passing across all 4 commands and duration parser.
- `pnpm --filter @ririko/bot typecheck`:
  - Exited with code 0 (no TypeScript errors).

## 3. Decisions & Gotchas
- Helper `resolveRole` was implemented to seamlessly parse roles from Discord slash interaction option (`getRole`), raw mention strings (`<@&123456>`), numeric role IDs (`123456`), or role names across both Slash and Prefix invocations.
- `parseRoleDuration` was explicitly named to avoid name collision with music duration parsing.

## 4. Next Steps
- Transition `TASK-1404` to `DONE`.
- Transition `TASK-1405` (`Gateway Event Listeners & Interaction Wiring (messageReactionAdd/Remove, guildMemberAdd, Interaction Router)`) to `IN_PROGRESS`.
