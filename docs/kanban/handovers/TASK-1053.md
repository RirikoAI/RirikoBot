# TASK-1053 Handover: Waifu TCG Info Hub, Onboarding Guide & Type Advantage Tutorial (/tcg-info)

## 1. Summary of Changes
- **Comprehensive Documentation (`docs/tcg-player-guide.md`)**:
  - Full player handbook and strategy guide covering:
    1. Getting Started & First Card (Starter kit via `/dungeon tutorial`, chat drops, marketplace).
    2. The Prologue Tutorial vs. The Seasonal Tower (Why players must run `/dungeon tutorial` before `/dungeon climb`).
    3. 7-Element Affinity Matrix & Status Effects (Fire > Ice > Earth > Lightning > Water > Fire, Light <> Shadow, 1.5x / 0.75x / 2.0x multipliers, 7 status effects).
    4. Equipments, Accessories & The +10 Enhancement System (6 gear slots, Crafting Dust, enhancement perks).
    5. PvE Seasonal Dungeon Tower (S1 Infernal Crucible, energy scaling, multi-layer wards, enrage clock).
    6. Peer-to-Peer (P2P) Trading System (`IN_TRADE` locking, safety, atomic dual-confirmation).
    7. Community Player Marketplace (`IN_MARKET` locking, 5% tax sink, 7-day auto-expiration).
    8. WaifuGuilds & Factions (Creation fee 5,000c, capacity $10 + \text{Lv} \times 2$, leveling, guild bank).
    9. Achievements & Multi-Asset Rewards (6 tracks, 5 tiers, 7 asset reward types).
    10. Complete Command Quick Reference Cheat Sheet.
- **Discord Bot Layer (`apps/bot`)**:
  - `info.command.ts` (`apps/bot/src/commands/tcg/info.command.ts`):
    - Dual-dispatch `/tcg-info` command with aliases `tcginfo`, `tcgguide`, `tcg-guide`, `card-guide`, `waifu-guide`.
    - 10 Topics: `overview`, `starter`, `elements`, `gear`, `tutorial`, `dungeon`, `trade`, `market`, `guild`, `achievements`.
    - Rich embed generator (`buildTcgInfoEmbed`) with formatted tables, color-coded themes, and clear syntax examples.
    - Interactive `StringSelectMenuBuilder` (`buildTcgInfoSelectMenu`) allowing players to change guide topics live in Discord.
    - Prefix argument fallback (`$tcg-info elements`, `$tcginfo trade`).
  - Integrated into `/card` (`apps/bot/src/commands/tcg/card.command.ts`):
    - Added `guide` and `info` actions to `/card` so players running `/card action:guide` or `/card info` directly access the TCG Info hub.
  - Registered in router (`apps/bot/src/main.ts`) and exported via `apps/bot/src/commands/tcg/index.ts`.

## 2. Verification & Quality Gates
- **Automated Tests**:
  - `apps/bot/src/commands/tcg/__tests__/info.command.test.ts` (14/14 passing).
  - All existing TCG bot command tests passing without regressions.
- **Static Analysis & Linting**:
  - `pnpm typecheck`: Clean across all 8 workspace packages (0 errors).
  - `pnpm eslint . --quiet`: 0 errors.

## 3. Decisions & Gotchas
- Interactive Component Collector: Uses `ComponentType.StringSelect` with a 2-minute timeout, disabling the select menu upon collector expiration.
- Topic Normalization: Any unrecognized topic input gracefully falls back to the `overview` hub embed.
- Cross-Command Reuse: `buildTcgInfoEmbed` and `buildTcgInfoSelectMenu` are exported for seamless reuse by `/card` and `/dungeon`.
