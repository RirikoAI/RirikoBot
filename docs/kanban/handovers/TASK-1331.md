# Handover: TASK-1331 (Welcomer & Farewell Dynamic Card Canvas & Schema)

## 1. Summary of Work
- Renamed legacy `welcome_configs` and `farewell_configs` to `guild_welcomer` and `guild_farewell` schemas in `packages/database/src/schema/pg/utilities.ts` and `sqlite/utilities.ts`.
- Added `backgroundUrl` and `textColor` fields to support custom dynamic card canvas generation.
- Updated database types and unit tests to expect `guildWelcomer` and `guildFarewell`.
- Created `WelcomerService` in `@ririko/services` (`packages/services/src/welcomer/welcomer.service.ts`).
- Implemented robust SSRF and DNS validation to ensure `backgroundUrl` cannot resolve to local IP addresses (`node:dns/promises` and `node:net`).
- Developed the core `renderCard()` method using `@napi-rs/canvas` to render high-resolution 1000x400 PNG welcome/farewell cards with the user's avatar, dynamic background, and text placement.
- Added comprehensive Vitest tests for the SSRF validation engine.

## 2. Verification
- `pnpm test` successfully executes the newly added `welcomer.service.test.ts`.
- Database schema tests successfully validate the presence of `guildWelcomer` and `guildFarewell` tables.
- `pnpm typecheck` passes across the monorepo.

## 3. Gotchas & Known Issues
- Image load failures (e.g. invalid public background URL) gracefully fallback to a generated gradient or solid background instead of throwing an error.

## 4. Next Steps
- Implement `TASK-1332`: Wire up the `guildMemberAdd` and `guildMemberRemove` Discord Gateway listeners to use this service, and implement the dual-dispatch `/welcomer` and `/farewell` commands to configure it.
