# Handover Note: [TASK-1401] Database Schemas & Dual-Dialect Repositories (AutoRoles, ReactionRoles, TempRoles)

## 1. Summary of Changes
- **SQLite & PostgreSQL Utilities Schema**:
  - Extended `reaction_roles` table with `type` (`EMOJI | BUTTON | SELECT_MENU`), `mode` (`TOGGLE | GIVE_ONLY | REMOVE_ONLY | UNIQUE`), `group_id`, `label`, and `description`.
  - Added `guild_auto_roles` table with `guild_id`, `human_role_ids`, `bot_role_ids`, `verification_role_id`, `verification_channel_id`, `verification_message_id`, `is_enabled`, `created_at`, `updated_at`.
  - Added `temporary_roles` table with `id`, `guild_id`, `user_id`, `role_id`, `expires_at`, `assigned_by`, `reason`, `created_at` with an index on `(expires_at)`.
- **Database Types**:
  - Exported `ReactionRole`, `NewReactionRole`, `GuildAutoRole`, `NewGuildAutoRole`, `TemporaryRole`, `NewTemporaryRole` in `packages/database/src/schema/types/index.ts`.
- **Dual-Dialect Repositories**:
  - Created `ReactionRoleRepository` in `packages/database/src/repositories/reaction-role.repository.ts`.
  - Created `AutoRoleRepository` in `packages/database/src/repositories/autorole.repository.ts`.
  - Exported both repositories in `packages/database/src/repositories/index.ts` and `packages/database/src/index.ts`.
- **DDL Synchronization**:
  - Regenerated `packages/database/src/schema/sqlite/ddl.ts` using `pnpm -F @ririko/database db:generate-ddl`.

## 2. Verification
- `pnpm -F @ririko/database db:generate-ddl` executed successfully (78 tables).
- `packages/database/src/repositories/reaction-role.repository.test.ts` passed (5 test cases).
- `packages/database/src/repositories/autorole.repository.test.ts` passed (6 test cases).
- `pnpm -F @ririko/database test` passed with 0 errors.
- `pnpm -F @ririko/database typecheck` passed with 0 errors.

## 3. Decisions & Gotchas
- `NewReactionRole` and `NewTemporaryRole` omit `id` making `id?: string` so that callers can either supply custom UUIDs or have the repository auto-generate them via `randomUUID()`.
- Both repositories properly implement all required lifecycle methods of `BaseRepository` (`findById`, `create`, `update`, `exists`, `delete`, `count`) across dual dialects.

## 4. Next Steps
- Transition `TASK-1401` to `DONE`.
- Transition `TASK-1402` (`AutoRole Engine: Join Roles, Verification & Temp Role Sweeper`) to `IN_PROGRESS`.
