# Handover Note: [TASK-0301] Discord.js 14 Client Factory with Gateway Intents, Partials & Cache Sweepers

- **Ticket Type & Points**: Task | 3 pts (Fibonacci)
- **Author / Agent**: lead-architect / discord
- **Status**: DONE
- **Timestamp**: 2026-09-15T09:05:00+08:00
- **Parent Story**: [`STORY-030: Discord Client Gateway Lifecycle, Sharding & REST V10 Harness`](file:///z:/Projects/ririko-v2-2026/docs/kanban/BOARD.md)

---

## 1. Summary of Work Accomplished
- **Production-Grade Discord Client Factory ([`packages/discord/src/client/factory.ts`](file:///z:/Projects/ririko-v2-2026/packages/discord/src/client/factory.ts))**:
  - Implemented `createDiscordClient(config)` building a fully configured Discord.js 14 `Client`.
  - **Default Gateway Intents (`DEFAULT_GATEWAY_INTENTS`)**:
    - `Guilds` (guild metadata, channels, roles)
    - `GuildMembers` (autoroles, join/leave events, member sync)
    - `GuildMessages` (command listening, moderation)
    - `GuildMessageReactions` (reaction roles, interactive components)
    - `GuildVoiceStates` (Auto Voice Channels / Join to Create, music voice streaming)
    - `MessageContent` (prefix command parsing and message validation)
  - **Default Partials (`DEFAULT_PARTIALS`)**:
    - `Message`, `Channel`, `Reaction`, `User`, `GuildMember` for full coverage of uncached historical events.
  - **Memory Protection via Cache Sweepers (`DEFAULT_SWEEPERS`)**:
    - Sweeps messages and threads older than 1 hour every 30 minutes to prevent long-running bot heap exhaustion.
  - **Safe Defaults**:
    - `allowedMentions: { parse: ['users'], repliedUser: true }` to block unauthorized role/everyone pings.
    - `failIfNotExists: false` to avoid uncaught Discord exceptions on deleted reply targets.
- **REST v10 Client Harness ([`packages/discord/src/rest/client.ts`](file:///z:/Projects/ririko-v2-2026/packages/discord/src/rest/client.ts))**:
  - Implemented `createRestClient(token, options)` utilizing Discord REST API v10 for standalone REST calls, application metadata queries, and command deployment.
- **Automated Verification ([`packages/discord/src/client/client.test.ts`](file:///z:/Projects/ririko-v2-2026/packages/discord/src/client/client.test.ts))**:
  - Verified default intent bitfield containment.
  - Verified partials and sweeper structures.
  - Verified custom intent/presence overrides.
  - Verified REST client construction.

---

## 2. Quality Gates Status
- `pnpm run build`: Passed (`tsc -b` across all packages)
- `pnpm -r run typecheck`: Passed (0 errors across 5 workspace projects)
- `pnpm run lint`: Passed (0 warnings, 0 errors)
- `pnpm run format:check`: Passed (Prettier clean)
- `pnpm run test`: **68 / 68 tests passing** across 13 test suites

---

## 3. Actionable Next Steps
- Transition `TASK-0301` to `DONE` and `TASK-0302: Gateway Lifecycle State Machine, Reconnection & Shard Health Monitoring` to `DONE`.
