# RIR-808 — fresh approved-stack prerequisite complete

Coordinator, 2026-09-14; five points, GR-005. User explicitly approved this prerequisite and a backed-up bootstrap. The canonical board was backed up before the change. The minimal additive decision-model change enabled normal `mutate` commands to record fresh consent and open the approved BATCH-003; no manual JSON transition or hook bypass was needed. No remote ref changed.

`Decision.kind = stack` names a closed parent and records actual consent for one new dependent scope, independently of its existing PR. Legacy combined defer-pr/stack decisions remain supported. Runtime shape/transition/Git validation accepts the new variant; stack consent cannot close a batch, substitute for publication approval or authorize multiple scopes. Existing parent PR #557 stays open at a04753a; the local receipt de7504b remains preserved on feat/RIR-110-publication-receipt, and the documentation topic inherits it by explicit consent.

A-004 / delivery-guard returned changes only in `tools/workboard/git-store.test.ts`: two real Git/store regressions use normal locked mutations to record a parent PR, close its batch, record fresh stack consent, groom/start/complete the child and reach its checkpoint. They verify exact published parent refs and child target, retained parent PR metadata, rejection of PR-created consent as stack authorization, rejection of parent republication and rejection of unapproved child publication. Worker ran **two targeted tests passed, 29 intentionally excluded**, plus file ESLint and strict repository typecheck. No timeout changes or workspace Git/board/publication mutations occurred.

Coordinator ran **33 model tests passed**, including closed-parent consent, unresolved-parent rejection, approval separation and decision reuse. Modified-code ESLint and strict typecheck passed; board and 91-section/35-criterion requirements validation passed. Initial test fixture lacked its required coordinator history entry; after correcting the fixture the suite passed. No runtime application feature changed and no new live/provider validation is claimed.

Changed types/model/Git validation, model/Git tests, workflow guidance and board metadata. RIR-005's separate frozen-parent integration issue remains unstarted. Accept A-004, move RIR-808 through review/done, then start only RIR-801. The rest of RIR-800 follows the sequential corpus plan in `.workboard/handoffs/RIR-800/002-corpus-plan.md`; stop at one epic PR checkpoint after all documentation leaves are complete.
