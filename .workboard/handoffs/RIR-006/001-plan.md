# RIR-006 — Docker engine repair

Coordinator, 2026-09-15. User approved pausing RIR-003 and a separate repair ticket. Read protocol/state/board, RIR-003 blocker and pause, blueprint BP-69/AC-32 and requirement ledger. RIR-003 is paused by D-008; BATCH-005 deferred via D-009 and preserved at2d3328e on its original branch. This repair starts at verified integration7ed2073 on fix/RIR-006-docker-engine and carries only administrative receipts.

Estimate five points before execution: Windows runtime/reparse-point investigation, minimal reversible recovery and bounded Linux-engine smoke verification with data preservation. Acceptance: diagnose exact failing endpoint; preserve existing Docker settings/data/volumes; restore Docker version/info and isolated container execution if safely possible; document tested recovery and remaining blockers. No factory reset, disk/WSL data deletion, prune or production changes. Track repo evidence only under .workboard; host operations use explicitly resolved Docker runtime paths. An upgrade or OS-level repair with material disruption requires a concrete review first.

Known failure: Desktop4.46.0 fails removing zero-byte Archive/ReparsePoint at <LOCALAPPDATA>/Docker/run/dockerInference. Ordinary Rename-Item fails; no Docker processes remain after previous cleanup. Inspect actual stopped state, parent directory/reparse attributes and filesystem metadata. Prefer preserving the runtime directory by same-parent rename and letting Desktop recreate transient endpoints if inspection confirms no persistent data there; do not touch Docker/wsl or settings. Verify exact resolved paths before any move. Only this ticket occupies the slot; stop at its delivery PR checkpoint before returning to RIR-003. No worker assignments.
