# RIR-005 — grooming and independent review

Coordinator, 2026-09-14. RIR-005 is the sole active13-point ticket under BATCH004. Baseline a27151bcf4ed3a72fdd3840011015acb17ae6048; new branch fix/RIR-005-integration-reconciliation. A026 and A027 are read-only assignments with no owned write paths. Their inspections were performed under board revision152; no specialist changed files, refs, state or ran tests.

A026 / integration-reviewer reproduced the frozen closed-board rejection by source inspection. Verified original/squash full-tree pairs a04753a/7f0fd9a and e09daed/f33b0c5; the two squashes are not linear ancestors. Verified the administrative-only receipt delta. Recommended explicit completed-source provenance, separate inherited-versus-repair ownership, strict ordinary stacks and actual-PR-head CI. Its implementation review identified imported squash history being misclassified and the early unresolved stack-target PR check. Both were corrected.

A027 / integration-tests designed the real-Git regression matrix and independently reviewed implementation. Required exact source board/repository/path authority, transitive source inclusion, single-parent equal-tree squashes, administrative-only baseline, retained ancestry at checkpoint, first-parent merge-resolution audit, failed-mutation atomicity and publication-consent separation. Found the missing next-state checkpoint verification and weak atomic-rejection fixture; both corrected. Recommended NUL-delimited --no-renames history traversal; implemented. Final read-only review found no remaining logic blocker for the pinned route; actual test results belong to coordinator.

The implementation uses one Git traversal for historical path names rather than a subprocess per commit. Original/squash ancestry already validated against source authority is excluded from repair history, while merge commits and the net repair delta remain checked. Ordinary nonintegration path checks retain prior behavior so incorporating legitimate upstream changes does not attribute them to the topic. The frozen closed-batch PR is still deliberately rejected; a new integration repair checkpoint supplies the safe route rather than rewriting history.

Test development exposed Windows five-second Git-fixture timeouts, one overly narrow parser-message expectation, and fixture/shared-state setup errors. These were corrected without weakening source or publication guards. Real Git fixture timeout is bounded at30seconds; service tests keep their normal limits. Fixture merge conflicts in the two canonical board files are resolved to the current board while preserving both source parents, matching the explicit reconciliation contract. Every rejected source, baseline and merge-resolution case remains a negative test.

Next: finish corrected tests, apply D006 integration resolution through the validated store, preserve local implementation commit, retain both verified squash commits using standard local merges and explicitly reviewed conflict resolutions, then validate the actual-head PR guard against develop/2.0.0-astra. Record exact checks and PR proposal before closing the bug. No publication/remote merge/new scope is authorized.
