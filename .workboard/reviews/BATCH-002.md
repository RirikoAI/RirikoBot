The original platform request was not preserved in Astra, and its roadmap did not map every requirement to evidence and remaining work. This story preserves the complete blueprint and adds a checked requirement ledger. It also makes the user-approved dependent story resolve its exact parent branch as the PR target, so the review excludes the prior governance chore's changes.

Batch **BATCH-002**, scope **RIR-110** (13-point story). Proposed head `feat/RIR-110-review-evidence` → immediate base `chore/RIR-001-work-governance` at `c1018829abf42fd2f8aad22948de343e51dc1e95`, in `RirikoAI/RirikoBot`. Eventual integration target remains `develop/2.0.0-astra`. The parent is preserved locally; publication requires separate explicit approval.

- Preserves the original 91-section blueprint and maps all sections plus the 35 exact final acceptance criteria to implementation, test or design references, existing backlog scopes and explicit gaps. CI rejects changed source text, incomplete mappings, stale generated output and invalid evidence references. References alone do not prove passing tests or live behavior.
- Adds a pinned comparison of Astra `801103c0e4c70eca6a380d7d1122b11234695bb1` and Gemini `1ba45ee3308b1bb5c7ecb4ea850c0009abb65d33`, distinguishing remote implementation, plans and unpublished local improvements. It records gaps in both implementations.
- Declares approved stacks atomically, isolates child changes against the preserved parent and binds publication approvals to the exact batch, head, base and target. Refuses moved parents, undeclared inherited scope, guessed local integration anchors, missing published parents and cross-use of parent/child approvals. Parent branch publication can be planned separately from the child checkpoint without changing board ownership.
- Updates every-session guidance and keeps the completed story's durable handoff visible at its delivery checkpoint.

Validation: **113 unit tests and 13 SQLite integration/CLI E2E tests passed; seven PostgreSQL cases skipped.** ESLint, strict typecheck, build, board and requirements validation passed. Exact commands and evidence are recorded in `.workboard/handoffs/RIR-110/003-completion.md` and `docs/testing.md`. Tests use disposable Git repositories/worktrees and do not publish branches or PRs. Application behavior and dependencies are unchanged; the legacy source remains read-only.

Known publication limit: the frozen parent's older PR checker rejects its closed batch. **RIR-005 remains unstarted backlog.** The reviewed option is to publish the preserved parent branch only, keep its own PR deferred, and open this story's PR against it after explicit approval. Parent integration needs a separate reviewed resolution; this PR does not claim to solve that issue or authorize a merge. Local hooks are bypassable and remote branch protection is not configured here.

Review artifacts: `docs/branch-comparison.md`, `docs/requirements.md`, `.workboard/handoffs/RIR-110/003-completion.md`. This prepared description is not evidence of publication approval or a created PR.
